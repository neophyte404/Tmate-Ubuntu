name: "Crave Run"

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 360
    steps:
      - name: setup
        run: |
          export TERM=xterm
          wget https://raw.githubusercontent.com/neophyte404/Tmate-Ubuntu/main/dependency.sh
          wget https://raw.githubusercontent.com/neophyte404/Tmate-Ubuntu/main/run.sh
          wget https://raw.githubusercontent.com/neophyte404/Tmate-Ubuntu/main/crave.conf
          bash dependency.sh

      - name: tmate ssh link
        run: |
          tmate -F
